export class Series {

    constructor(public id: number, 
        public nombre: string, public canal: string,
        public temporada: number, public resumen: string,
        public link: string, public imagen: string) {
 
    }
}

